# HotShop

A minimal JavaFX starter using Java 25 and Gradle 9.1.0.

Run `./gradlew run` (Windows: `.\gradlew.bat run`).

- [User Guide](docs/UserGuide.md)
- [Developer Guide](docs/DeveloperGuide.md)

Build with `./gradlew shadowJar`; output: `release/HotShop.jar`.
